import { styled } from "@/styles";

export const DefaultLayoutContainer = styled("div", {
  height: "100vh",
});

export const LayoutWithPadding = styled("div", {
  display: "flex",
  height: "100%",
  padding: 20,
  gap: 96,
});
